name: Build iOS App

on:
  workflow_dispatch:
    inputs:
      target:
        description: 'Build Target'
        required: true
        default: 'simulator'
        type: choice
        options:
          - simulator
          - unsigned-ipa
  push:
    tags:
      - 'v*'

concurrency:
  group: build-ios-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-ios:
    name: Build iOS (${{ inputs.target || (github.ref == 'refs/heads/main' && 'simulator') || 'unsigned-ipa' }})
    runs-on: macos-26

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Select Xcode 26 Version
        run: |
          # Capacitor 8's SPM-distributed core requires Xcode 26 (Swift 6.2) —
          # building with an older toolchain drops APIs plugins compile
          # against (getArray, CAPPluginCall.reject, etc.) with no warning.
          # https://github.com/ionic-team/capacitor/issues/8333
          if [ -d "/Applications/Xcode_26.6.app" ]; then
            sudo xcode-select -switch /Applications/Xcode_26.6.app
          elif [ -d "/Applications/Xcode_26.5.app" ]; then
            sudo xcode-select -switch /Applications/Xcode_26.5.app
          elif [ -d "/Applications/Xcode_26.0.1.app" ]; then
            sudo xcode-select -switch /Applications/Xcode_26.0.1.app
          fi
          xcodebuild -version

      - name: Install NPM Dependencies
        run: npm ci

      - name: Align SPM Plugin Dependencies to 8.2.0
        run: node scripts/align-spm-versions.mjs

      - name: Build Web Production Bundle
        env:
          VITE_BASE_PATH: /
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
          VITE_TURNSTILE_SITE_KEY: ${{ secrets.VITE_TURNSTILE_SITE_KEY }}
        run: npm run build

      - name: Sync Native Version
        env:
          CM_BUILD_NUMBER: ${{ github.run_number }}
        run: node scripts/sync-native-version.mjs

      - name: Sync Capacitor iOS
        run: npx cap sync ios

      - name: Build iOS Simulator App (.app)
        if: ${{ inputs.target == 'simulator' || (github.event_name == 'push' && !startsWith(github.ref, 'refs/tags/')) || (github.event_name == 'workflow_dispatch' && inputs.target == 'simulator') }}
        run: |
          xcodebuild \
            -project ios/App/App.xcodeproj \
            -scheme App \
            -destination 'generic/platform=iOS Simulator' \
            -configuration Debug \
            -derivedDataPath build/ios \
            CODE_SIGNING_ALLOWED=NO

      - name: Build Unsigned iOS Archive (.ipa)
        if: ${{ inputs.target == 'unsigned-ipa' || startsWith(github.ref, 'refs/tags/') }}
        run: |
          xcodebuild \
            -project ios/App/App.xcodeproj \
            -scheme App \
            -configuration Release \
            -archivePath build/ios/App.xcarchive \
            archive \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY=""

      - name: Zip iOS Build Artifacts
        run: |
          mkdir -p build/artifacts
          if [ -d "build/ios/Build/Products/Debug-iphonesimulator/App.app" ]; then
            cd build/ios/Build/Products/Debug-iphonesimulator
            zip -r ../../../../../build/artifacts/TripTracker-Simulator.app.zip App.app
            cd -
          fi
          if [ -d "build/ios/App.xcarchive" ]; then
            cd build/ios
            zip -r ../artifacts/TripTracker.xcarchive.zip App.xcarchive
            cd -
          fi

      - name: Upload iOS Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ios-build-${{ inputs.target || 'simulator' }}-${{ github.run_number }}
          path: build/artifacts/*
          retention-days: 14
